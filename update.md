# z_ipc 库重大更新日志

**版本**：v1.2.0  
**发布日期**：2026-08-24  
**适用范围**：全平台（Linux / Windows）  
**兼容性**：**二进制不兼容**（共享内存协议新增头部，需同时升级客户端和服务端）

---

## 📌 版本定位

本次是一次**架构级稳定性与安全性升级**，重点解决：

- **死锁与阻塞风险**：所有发送操作改为非阻塞（`try_send`），所有接收操作增加超时机制。
- **数据完整性**：共享内存传输增加魔数 + CRC32 校验，防止部分写入或篡改导致的数据损坏。
- **资源泄漏**：引入跨平台共享内存租约（RAII），彻底解决进程崩溃后孤儿共享内存残留问题。
- **可观测性**：增加服务端 worker 心跳监控、详细日志，便于定位问题。

---

## ✅ 核心修复与新增功能

### 1. 彻底消除发送阻塞（P0）

**问题**：此前 `client::send` 和 `server::send` 使用阻塞式 `send()`，当队列满时会导致调用线程永久挂起，造成程序卡死。  
**修复**：
- 所有发送操作统一替换为 `try_send()`，失败时立即返回 `IPC_ERR_BUSY`，并清理已创建的共享内存。  
- 客户端 RPC 等待响应时使用 `std::shared_future::wait_for` 并轮询取消标志，超时返回 `IPC_ERR_TIMEOUT`。

**影响范围**：`call_binary`、`notify_binary`、`send_notify_binary`、`send_response`。

---

### 2. 共享内存数据完整性校验（新增）

**问题**：发送方写入共享内存后，接收方直接读取并假定完整，无法检测发送方崩溃（写入一半）、数据被篡改等场景。  
**修复**：
- 在每块共享内存头部增加固定结构 `ShmHeader`：
  ```cpp
  struct ShmHeader {
      uint32_t magic;      // 固定 0xDEC0DEAD
      uint32_t checksum;   // CRC32 of payload
      uint32_t data_len;   // 实际数据大小（不含头部）
  };
  ```
- 发送方先写入头部（含 CRC32），再写入数据；接收方读取后验证魔数和校验和，不匹配则拒绝数据并返回 `IPC_ERR_RECEIVE`。
- 零大小数据（`__ZERO__`）跳过校验。

**影响**：协议升级，需同时更新客户端和服务器。

---

### 3. 共享内存租约机制（RAII 清理）

**问题**：进程崩溃后，已创建的共享内存可能永久残留（尤其在 Linux 下），累积后导致系统资源耗尽。  
**修复**：
- 实现 `SharedMemoryLease` 类，在 `/dev/shm`（Linux）或 `%TEMP%`（Windows）下创建同名 `.lease` 文件，记录创建时间戳。
- 析构时自动删除 lease 和共享内存；静态方法 `cleanup_stale()` 可扫描并清理超过 30 秒的孤儿资源。
- Windows 下使用临时文件作为弱租约，虽不完美但可减少残留。

---

### 4. 客户端 RPC 取消机制

- 新增 `IpcClient::cancel()` 方法，通过原子标志 `cancel_requested_` 中断正在等待的 `call_binary`，返回 `IPC_ERR_CANCELED`。
- 取消不会终止已发送的请求，仅停止本地等待。

---

### 5. 服务端 Worker 健康监控

- 每个 worker 线程周期性更新心跳时间戳（`worker_last_active_`）。
- 独立监控线程每秒检查一次，若某 worker 超过 5 秒未更新，则记录警告日志（便于运维介入）。
- 不自动重启 worker，避免状态不一致风险。

---

### 6. 安全加固

- 队列名称严格限制为字母、数字、下划线（禁止 `/`、`.` 等路径字符），防止路径遍历攻击。
- 参数防御：`thread_count`、`max_queue_length`、`max_msg_size` 在 `start()` 和 `ipc_server_create_ex` 中进行截断和归一化。
- 所有原子操作明确使用 `acquire/release` 内存序，避免默认 `seq_cst` 性能损失。

---

### 7. 线程生命周期管理优化

- 客户端析构和 `disconnect` 中不再依赖 `ReceiverGuard`，直接对线程进行 `join`（设 500ms 超时）。
- 服务器 `stop()` 中工作线程先等待 5 秒再 `join`，极端超时后 `detach` 并记录警告（作为最后手段）。
- 移除所有“每个响应创建独立清理线程”的不可控设计。

---

## 🧪 测试验证

本版本已通过以下场景验证：

| 测试场景 | 结果 |
|----------|------|
| 高并发通知（每秒 1000 条，持续 10 分钟） | 无阻塞，无内存泄漏 |
| 模拟发送方崩溃（强制终止进程） | 租约机制在 30 秒后自动清理残留 shm |
| 数据篡改（手动修改共享内存内容） | 接收方校验失败，拒绝数据并返回错误 |
| 服务端/客户端异常重启 | 租约清理正常，无资源残留 |
| 极端参数注入（空队列名、超大消息、负线程数） | 库返回错误码，不崩溃 |

---

## ⚠️ 升级注意事项

- **协议不兼容**：由于共享内存头部变更，v1.2.0 无法与旧版本通信，需全量升级。
- **建议迁移步骤**：
  1. 停止所有服务端和客户端进程。
  2. 手动清理旧版遗留的共享内存（可运行 `ipc_cleanup` 或重启系统）。
  3. 部署新版本库并重启服务。

---

## 📁 变更文件清单

| 文件 | 主要变更 |
|------|----------|
| `z_ipc_api.h` | 新增 `IPC_ERR_CANCELED` |
| `z_ipc_client_impl.h` | 新增 `cancel()`、取消标志 |
| `z_ipc_client_impl.cpp` | 全面重写：非阻塞发送、CRC 校验、租约、内存序、线程管理 |
| `z_ipc_server_impl.h` | 新增心跳数组、监控线程声明 |
| `z_ipc_server_impl.cpp` | 全面重写：非阻塞发送、CRC 校验、租约、监控、参数防御 |
| `z_ipc_md5.h` | 无变更（仅用于日志） |

---

## 📅 后续规划

- **v1.3.0**：将控制消息从字符串格式升级为二进制协议头，进一步提升性能和安全性。
- **v2.0.0**：引入共享内存环缓冲区，支持零拷贝批量传输。

---

本版本已通过全部回归测试，可投入生产使用。如有问题，请及时联系开发团队。
